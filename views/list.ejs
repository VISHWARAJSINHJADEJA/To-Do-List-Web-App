<%- include("header") -%>
<!-- 
    <% if(listTitle =="Saturday" || listTitle=="Sunday") { %>
        <h2 style="color:purple"><%=listTitle%> ToDO List</h2>
        <% } else{  %>
            <h2 style="color:blue"><%=listTitle%> ToDO List</h2>
    <%  } %> -->
  <div class="box" id="heading">
      <h1><%=listTitle%></h1>
  </div>
    
<div class="box">

    <!-- <li>Buy Food</li>
    <li>Cook Food</li>
    <li>Eat Food</li> -->

    <% newListItems.forEach(function(item){ %>

    <form action="/delete" method="post">
        <div class="item">
            <input type="checkbox" name="checkbox" value="<%=item._id%>" onChange="this.form.submit()">
            <p> <%= item.name %></p>
        </div>
        <input type="hidden" name="listName" value="<%= listTitle %>" id="">
    </form>
        
  <% }) %>
        
      <form class="item" action="/" method="post">  <!-- "/" is a home route -->
          <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
          <button type="submit" name="list" value="<%=listTitle%>">+</button>
      </form>
    
</div>

<%- include("footer") -%>